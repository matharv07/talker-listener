ROS workspace = ros2_ws, package name = rostest, output node = talker, display node = listener, topic name = chatter

commands:    ros2 run rostest talker    -    start talker node
             ros2 run rostest listener  -    start listener node
